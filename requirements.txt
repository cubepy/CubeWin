# Essentials, not the PySide6 meta-package: that also pulls PySide6-Addons,
# a 316 MB download of Qt3D/Charts/Multimedia/WebEngine this app never imports.
# The only Qt modules used anywhere in the tree are QtCore, QtGui, QtWidgets,
# QtSvg and QtTest, all of which are in Essentials.
PySide6-Essentials>=6.7
requests>=2.31
PySocks>=1.7.1
psutil>=5.9
# WinDivert bindings: Windows-only, and not installable elsewhere. Without the
# marker the whole install fails on macOS before anything else is fetched.
pydivert==3.1.3; sys_platform == "win32"
scapy>=2.5
PyInstaller>=6.0
pytest>=8.0
